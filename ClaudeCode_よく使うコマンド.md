# Claude Code よく使うコマンド・ショートカット集

> 守りの設定講義で言及されたコマンド + 一般的によく使われるコマンドをまとめたチートシート。
> ターミナルで `/` を入力すると候補が表示される。

---

## 🔥 最頻出（毎日使う）

### コンテキスト管理

| コマンド | 効果 | 使いどころ |
|---|---|---|
| `/clear` | やり取り内容を完全リセット | トピック切り替え時。履歴は `/resume` で復元可能 |
| `/compact` | やり取りを圧縮して文脈を維持 | 同じ話題を続けたいが容量を節約したいとき |
| `/resume` | 過去セッション一覧から復帰 | `/clear` 後に前のやり取りに戻りたいとき |
| `/branch` | 現在のやり取りを複製して分岐 | 同じ文脈から別の話題A・Bに展開したいとき |

> **自動コンパクト**: 20万トークン到達時にClaudeが自動実行。`/clear` しても履歴は消えない。

### モデル・思考深度

| コマンド | 効果 | 使いどころ |
|---|---|---|
| `/model` | モデル選択画面を開く | Opus / Sonnet / Haiku の切り替え |
| `/model opus-plan` | Opus Plan モードを有効化（隠しコマンド） | **基本これ推奨**。計画はOpus、実行はSonnet |
| `/fast` | Opus 4.6の高速モード切替 | 思考スピード優先のとき |

`/model` 画面で左右キー → Effort（思考の深さ）調整: **low / medium / high / max**（推奨: medium）

---

## 🛠 設定・状態確認

| コマンド | 効果 |
|---|---|
| `/config` | 設定画面を開く（テーマ、モデルなど） |
| `/status` | 現在の接続・認証状態を表示 |
| `/cost` | このセッションの利用コスト・トークン数を表示 |
| `/permissions` | パーミッション設定を確認・変更 |
| `/help` | ヘルプ表示 |
| `/rename` | 現在のセッションに名前を付ける |
| `/init` | このディレクトリに CLAUDE.md を新規作成 |
| `/memory` | MEMORY.md の内容を確認・編集 |

---

## 🔌 拡張機能

| コマンド | 効果 |
|---|---|
| `/agents` | サブエージェント一覧表示・管理 |
| `/mcp` | MCP接続の管理（追加・削除・確認） |
| `/plugin` | プラグインの管理（インストール・有効化） |
| `/ide` | IDE（Cursor/VSCode/Zed）との接続管理 |
| `/install-github-app` | GitHub App をリポジトリに追加 |

---

## 🔍 レビュー・品質チェック

| コマンド | 効果 |
|---|---|
| `/review` | プルリクエストをレビュー |
| `/security-review` | 現ブランチの変更をセキュリティ観点でレビュー |
| `/ultrareview` | マルチエージェントでブランチ全体をクラウドレビュー（有料） |

---

## 💡 入力ショートカット（コマンドではないが超便利）

| 入力 | 効果 |
|---|---|
| `! <cmd>` | 行頭に `!` を付けるとシェルコマンドをそのまま実行（例: `! git status`） |
| `# <text>` | 行頭に `#` を付けるとメモリ（MEMORY.md）に追加 |
| `@<file>` | ファイル参照を埋め込む（パス補完あり） |

---

## ⌨️ キーボードショートカット

| キー | 効果 |
|---|---|
| `Esc` | 現在の処理を中断（思考中・実行中を止める） |
| `Esc` 2回連打 | 直前のメッセージまで巻き戻し（rewind） |
| `Shift + Tab` | モード切替（normal / acceptEdits / plan / bypassPermissions） |
| `Ctrl + R` | verbose（詳細）表示のオン・オフ |
| `Ctrl + O` | 直近の圧縮（compact）サマリを全文表示 |
| `Ctrl + C` | セッション終了 |
| `Ctrl + L` | ターミナル画面をクリア（履歴は保持） |
| `↑` / `↓` | 入力履歴を遡る |

---

## 🎨 個人化スキル（太一さんの場合）

`/` 補完で出るユーザー定義スキル。詳細は `skills-cursor/SKILLS_INDEX.md` 参照。

| コマンド | 効果 |
|---|---|
| `/ss` | クリップボードのスクショを読み込み |
| `/project` | プロジェクトの進捗.mdを作成・更新 |
| `/knowledge-import` | 記事URL等をナレッジ化して保存 |
| `/shell <cmd>` | シェル直叩き |

---

## 🎭 小ネタ

| コマンド | 効果 |
|---|---|
| `/stickers` | Claude Code のステッカーが購入できる |

---

## 🚨 やってはいけない（守りの設定より）

- `.env` の中身を「これコピペして」と頼む → コンテキストにキーが残り流出リスク
- `settings.json` に直接APIキーを書く → 起動のたびにコンテキストに載る
- MCPを勝手に接続する → 接続先サービス全体に広範な権限が付与される（特にNotion MCP）
- `settings.json` 編集後にセッション再起動を忘れる → 古い設定のまま動く可能性

---

## 太一さんの推奨ルーティン

1. **セッション開始時** → `/status` で接続確認、必要なら `/model opus-plan` に切替
2. **トピック切り替え時** → `/clear`（後で戻りたいなら `/branch`）
3. **コンテキスト40%超** → `/compact` を検討
4. **3ステップ以上のタスク開始時** → `/project` で進捗.mdを作成
5. **セッション中断時** → `/project` で中断ポイントを更新
6. **PR出した後** → `/review` または `/security-review`

---

> 最終更新: 2026-05-13 ｜ 出典: 守りの設定講義 + Claude Code 公式ドキュメント + 実運用
